export interface allGoals {
    goal:string,
    fromDate:any,
    toDate:any,
    color:any,
  // tasks:any
  }

  export interface allTasks {
    goalsHead:string,
    goalsTasks:any
  }


  export interface goalTasks {

    tasks:any[]
  }
